name: Format Rust Phase4

on:
  push:
    branches: [agent/rust-revisions-phase4]
    paths:
      - "rust/crates/asr-contracts/**"
      - ".github/workflows/format-rust-phase4.yml"

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/rust-revisions-phase4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all
      - name: Commit formatting
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add rust/crates/asr-contracts
          git commit -m "Format revision validator"
          git push origin HEAD:agent/rust-revisions-phase4
