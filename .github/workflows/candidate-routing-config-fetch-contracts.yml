name: Candidate Routing Config Fetch Contracts

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/candidate-routing-config-fetch-contracts.yml
      - .github/workflows/candidate-request-gateway.yml
      - .github/workflows/candidate-package-evaluate-v2.yml
      - scripts/ci/fetch-source-routing-config.sh
  pull_request:
    paths:
      - .github/workflows/candidate-routing-config-fetch-contracts.yml
      - .github/workflows/candidate-request-gateway.yml
      - .github/workflows/candidate-package-evaluate-v2.yml
      - scripts/ci/fetch-source-routing-config.sh
  workflow_dispatch:

permissions:
  contents: read
  actions: write
  issues: write

jobs:
  contracts:
    name: Validate tri-state source routing fetch
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install YAML parser
        run: python -m pip install --disable-pip-version-check pyyaml
      - name: Validate helper syntax
        run: bash -n scripts/ci/fetch-source-routing-config.sh

      - name: Accept successful encoded routing config
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/fake-bin
          cat > /tmp/fake-bin/gh <<'SH'
          #!/usr/bin/env bash
          set -euo pipefail
          [[ "${1:-}" == api && "${2:-}" == --include ]]
          [[ "${3:-}" == /repos/owner/source/contents/.jpapt/hf-bucket.yml ]]
          encoded="$(printf '%s' $'hf_bucket: owner/source-bucket\ndatasets:\n  default_source: repository\n  repository_dataset: owner/evalset\n' | base64 -w0)"
          printf 'HTTP/2.0 200 OK\r\ncontent-type: application/json\r\n\r\n{"content":"%s"}\n' "$encoded"
          SH
          chmod +x /tmp/fake-bin/gh
          PATH="/tmp/fake-bin:$PATH" \
            GH_FALLBACK_TOKEN=test-token \
            bash scripts/ci/fetch-source-routing-config.sh owner/source /tmp/source.json
          jq -e '.hf_bucket == "owner/source-bucket"' /tmp/source.json >/dev/null
          jq -e '.datasets.default_source == "repository"' /tmp/source.json >/dev/null
          jq -e '.datasets.repository_dataset == "owner/evalset"' /tmp/source.json >/dev/null

      - name: Treat visible repository file 404 as missing config
        shell: bash
        run: |
          set -euo pipefail
          cat > /tmp/fake-bin/gh <<'SH'
          #!/usr/bin/env bash
          set -euo pipefail
          [[ "${1:-}" == api && "${2:-}" == --include ]]
          case "${3:-}" in
            /repos/owner/source/contents/.jpapt/hf-bucket.yml)
              printf 'HTTP/2.0 404 Not Found\r\ncontent-type: application/json\r\n\r\n{"message":"Not Found"}\n'
              exit 1
              ;;
            /repos/owner/source)
              printf 'HTTP/2.0 200 OK\r\ncontent-type: application/json\r\n\r\n{"full_name":"owner/source"}\n'
              ;;
            *)
              echo "unexpected API target: ${3:-}" >&2
              exit 99
              ;;
          esac
          SH
          chmod +x /tmp/fake-bin/gh
          PATH="/tmp/fake-bin:$PATH" \
            GH_FALLBACK_TOKEN=test-token \
            bash scripts/ci/fetch-source-routing-config.sh owner/source /tmp/missing.json
          jq -e 'type == "object" and length == 0' /tmp/missing.json >/dev/null

      - name: Exercise live GitHub CLI 404 plus visible-repository fallback
        shell: bash
        env:
          GH_FALLBACK_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          bash scripts/ci/fetch-source-routing-config.sh "$GITHUB_REPOSITORY" /tmp/live-missing.json
          jq -e 'type == "object" and length == 0' /tmp/live-missing.json >/dev/null

      - name: Reject config 404 when repository visibility is also hidden
        shell: bash
        run: |
          set -euo pipefail
          cat > /tmp/fake-bin/gh <<'SH'
          #!/usr/bin/env bash
          set -euo pipefail
          printf 'HTTP/2.0 404 Not Found\r\ncontent-type: application/json\r\n\r\n{"message":"Not Found"}\n'
          exit 1
          SH
          chmod +x /tmp/fake-bin/gh
          rm -f /tmp/hidden.json
          if PATH="/tmp/fake-bin:$PATH" GH_FALLBACK_TOKEN=test-token \
            bash scripts/ci/fetch-source-routing-config.sh owner/source /tmp/hidden.json; then
            echo 'masked private/inaccessible repository unexpectedly treated as missing config' >&2
            exit 1
          fi
          test ! -e /tmp/hidden.json

      - name: Reject authorization and transient API failures
        shell: bash
        run: |
          set -euo pipefail
          for code in 401 403 429 500 503; do
            cat > /tmp/fake-bin/gh <<SH
          #!/usr/bin/env bash
          printf 'HTTP/2.0 $code Failure\r\ncontent-type: application/json\r\n\r\n{"message":"failure"}\n'
          exit 1
          SH
            chmod +x /tmp/fake-bin/gh
            rm -f /tmp/failure.json
            if PATH="/tmp/fake-bin:$PATH" GH_FALLBACK_TOKEN=test-token \
              bash scripts/ci/fetch-source-routing-config.sh owner/source /tmp/failure.json; then
              echo "HTTP $code unexpectedly treated as missing config" >&2
              exit 1
            fi
            test ! -e /tmp/failure.json
          done

      - name: Reject transport failure without HTTP response
        shell: bash
        run: |
          set -euo pipefail
          cat > /tmp/fake-bin/gh <<'SH'
          #!/usr/bin/env bash
          echo 'simulated transport failure' >&2
          exit 1
          SH
          chmod +x /tmp/fake-bin/gh
          rm -f /tmp/transport.json
          if PATH="/tmp/fake-bin:$PATH" GH_FALLBACK_TOKEN=test-token \
            bash scripts/ci/fetch-source-routing-config.sh owner/source /tmp/transport.json; then
            echo 'transport failure unexpectedly treated as missing config' >&2
            exit 1
          fi
          test ! -e /tmp/transport.json

      - name: Reject malformed repository identity before gh
        shell: bash
        run: |
          set -euo pipefail
          cat > /tmp/fake-bin/gh <<'SH'
          #!/usr/bin/env bash
          echo invoked > /tmp/unexpected-routing-gh
          exit 99
          SH
          chmod +x /tmp/fake-bin/gh
          rm -f /tmp/unexpected-routing-gh
          for repository in ./repo ../repo owner/. owner/.. owner/repo/extra; do
            if PATH="/tmp/fake-bin:$PATH" bash scripts/ci/fetch-source-routing-config.sh "$repository" /tmp/invalid.json; then
              echo "invalid repository unexpectedly accepted: $repository" >&2
              exit 1
            fi
          done
          test ! -e /tmp/unexpected-routing-gh

      - name: Assert production workflows use fail-closed helper before Rust resolution
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path

          helper = 'bash scripts/ci/fetch-source-routing-config.sh "$SOURCE_REPOSITORY" /tmp/source.json'
          for path, resolver in [
              ('.github/workflows/candidate-request-gateway.yml', 'Resolve request with Rust contract'),
              ('.github/workflows/candidate-package-evaluate-v2.yml', 'Resolve with Rust contract'),
          ]:
              text = Path(path).read_text(encoding='utf-8')
              assert helper in text, path
              assert 'contents/.jpapt/hf-bucket.yml" --jq .content 2>/dev/null || true' not in text, path
              assert text.index(helper) < text.index(resolver), path
          PY

  main_protocol_acceptance:
    if: ${{ github.event_name == 'pull_request' && github.head_ref == 'agent/main-protocol-acceptance-20260820' }}
    name: Drive deployed main protocol acceptance
    environment: Private-Secrets
    runs-on: ubuntu-latest
    timeout-minutes: 40
    env:
      MAIN_REF: main
      SOURCE_REPOSITORY: largoyo/Premiere-AutoProcess-Plugin
      RECEIPT_REPOSITORY: bie3yeik-lgtm/jpapt-protocol-e2e-source
      HF_BUCKET: gawohok7/tf-v2.2-onnx-dev-bucket
    steps:
      - name: Run canonical bootstrap, readiness, and E2E
        id: acceptance
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          dispatch_and_wait() {
            local workflow="$1"
            local body="$2"
            local timeout="$3"
            local before run_id status conclusion deadline
            before="$(gh api "/repos/$GITHUB_REPOSITORY/actions/workflows/$workflow/runs?branch=$MAIN_REF&event=workflow_dispatch&per_page=1" --jq '.workflow_runs[0].id // 0')"
            gh api --method POST "/repos/$GITHUB_REPOSITORY/actions/workflows/$workflow/dispatches" --input "$body"
            run_id=''
            for _ in $(seq 1 40); do
              run_id="$(gh api "/repos/$GITHUB_REPOSITORY/actions/workflows/$workflow/runs?branch=$MAIN_REF&event=workflow_dispatch&per_page=5" --jq ".workflow_runs[] | select(.id > $before) | .id" | head -n1)"
              [[ -z "$run_id" ]] || break
              sleep 2
            done
            [[ -n "$run_id" ]] || { echo "::error::new run not found for $workflow"; return 1; }
            echo "[acceptance] workflow=$workflow run_id=$run_id"
            deadline=$((SECONDS + timeout))
            while (( SECONDS < deadline )); do
              read -r status conclusion < <(gh api "/repos/$GITHUB_REPOSITORY/actions/runs/$run_id" --jq '[.status, (.conclusion // "")] | @tsv')
              if [[ "$status" == completed ]]; then
                [[ "$conclusion" == success ]] || { echo "::error::$workflow failed: run=$run_id conclusion=$conclusion"; return 1; }
                printf '%s' "$run_id"
                return 0
              fi
              sleep 5
            done
            echo "::error::$workflow timed out: run=$run_id"
            return 1
          }

          jq -n --arg ref "$MAIN_REF" --arg repo "$RECEIPT_REPOSITORY" \
            '{ref:$ref,inputs:{repository:$repo,adopt_existing:"false"}}' > /tmp/bootstrap.json
          bootstrap_run="$(dispatch_and_wait candidate-receiver-bootstrap.yml /tmp/bootstrap.json 900)"

          jq -n --arg ref "$MAIN_REF" --arg repo "$RECEIPT_REPOSITORY" --arg bucket "$HF_BUCKET" \
            '{ref:$ref,inputs:{receipt_repository:$repo,hf_bucket:$bucket,require_managed_installation:"true"}}' > /tmp/readiness.json
          readiness_run="$(dispatch_and_wait candidate-protocol-readiness.yml /tmp/readiness.json 900)"

          jq -n --arg ref "$MAIN_REF" --arg source "$SOURCE_REPOSITORY" --arg repo "$RECEIPT_REPOSITORY" --arg bucket "$HF_BUCKET" \
            '{ref:$ref,inputs:{receipt_repository:$repo,hf_bucket:$bucket,source_repository:$source,timeout_seconds:"600"}}' > /tmp/e2e.json
          e2e_run="$(dispatch_and_wait candidate-protocol-e2e.yml /tmp/e2e.json 1200)"

          {
            echo "bootstrap_run=$bootstrap_run"
            echo "readiness_run=$readiness_run"
            echo "e2e_run=$e2e_run"
          } >> "$GITHUB_OUTPUT"

      - name: Record deployed-main positive-path evidence
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          BOOTSTRAP_RUN: ${{ steps.acceptance.outputs.bootstrap_run }}
          READINESS_RUN: ${{ steps.acceptance.outputs.readiness_run }}
          E2E_RUN: ${{ steps.acceptance.outputs.e2e_run }}
        run: |
          set -euo pipefail
          gh issue comment 70 --repo "$GITHUB_REPOSITORY" --body "Deployed-main positive-path acceptance completed successfully.\n\n- main bootstrap run: \`$BOOTSTRAP_RUN\`\n- main readiness run: \`$READINESS_RUN\`\n- main zero-compute cross-repository E2E run: \`$E2E_RUN\`\n- source: \`$SOURCE_REPOSITORY\`\n- receiver: \`$RECEIPT_REPOSITORY\`\n- HF Bucket: \`$HF_BUCKET\`\n- compute boundary: V2 \`dry_run=true\`; no model inference, GPU, Docker evaluation, or HF Jobs workload.\n\nIssue #70 remains open until its required duplicate/idempotence and negative-routing assertions are also recorded."
