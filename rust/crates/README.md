# Parakeet ASR Rust crates scaffold

This archive contains the `rust/crates/` structure discussed in the project design.

The Rust source files are intentionally minimal placeholders. Their responsibilities and implementations are to be filled in subsequent steps.
