# Candidate protocol Rust validation

Validation-only marker. This file exists only on the temporary PR branch so pull-request CI compiles, formats, lints, and tests the current `main` Rust protocol implementation without changing production behavior.
